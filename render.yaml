services:
  - type: web
    name: metabase
    env: docker
    repo: https://github.com/render-examples/metabase.git # optional
    plan: standard # ensure that metabase has at least 2GB of RAM (defaults to starter)
    autoDeploy: false
    envVars:
      - key: ep-red-frog-74355594-pooler.us-east-1.postgres.vercel-storage.com
        fromDatabase:
          name: elephant
          property: connectionString
databases:
  - name: elephant
    databaseName: metabasedb
    ipAllowList: [] # only allow internal connections
